
    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.shiping_address) }}
    {{ form_row(form.seller_id) }}
    {{ form_row(form.base_address) }}
    {{ form_row(form.product_id) }}
    <input type = "submit" name = "submit" value = "Отправить">
    {{ form_end(form) }}