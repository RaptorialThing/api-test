
    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.default_shipping_address) }}
    {{ form_row(form.seller_id) }}
    <input type = "submit" name = "submit" value = "Отправить">
    {{ form_end(form) }}
